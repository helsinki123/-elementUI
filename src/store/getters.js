const getters = {
    // activeIndex: state => state.app.activeIndex,
    // tabClickStatus: state => state.app.tabClickStatus,
    // queryType: state => state.app.queryType,
    // userInfo: state => state.user.userInfo,
    // videoSbzcdm: state => state.app.videoSbzcdm,
    // videoDevice: state => state.app.videoDevice,
    // videoIndex: state => state.app.videoIndex,
    // videoIndexNew: state => state.app.videoIndexNew,
    testName: state => state.common.testName,

};
export default getters;
